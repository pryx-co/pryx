{
  "name": "pryx-tui",
  "version": "0.1.0",
  "type": "module",
  "module": "index.tsx",
  "scripts": {
    "build": "bun -e 'import solidPlugin from \"@opentui/solid/bun-plugin\"; const platform = process.platform === \"win32\" ? \"windows\" : process.platform; const target = `bun-${platform}-${process.arch}`; const result = await Bun.build({ entrypoints: [\"./index.tsx\"], target: \"bun\", outdir: \".\", plugins: [solidPlugin], compile: { target, outfile: \"pryx-tui\" } }); if (!result.success) process.exit(1);'",
    "build:ci": "bun -e 'import solidPlugin from \"@opentui/solid/bun-plugin\"; const result = await Bun.build({ entrypoints: [\"./index.tsx\"], target: \"bun\", outdir: \"dist\", plugins: [solidPlugin] }); if (!result.success) process.exit(1);'",
    "dev": "bun index.tsx",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "c8 --reporter=text --reporter=json --reporter=html --reporter=lcov vitest run"
  },
  "dependencies": {
    "@opentui/core": "^0.1.75",
    "@opentui/solid": "^0.1.75",
    "@types/js-yaml": "^4.0.9",
    "effect": "latest",
    "js-yaml": "^4.1.1",
    "solid-js": "^1.9.11",
    "ws": "latest"
  },
  "devDependencies": {
    "@testing-library/react": "^14.2.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/ws": "latest",
    "bun-types": "latest",
    "c8": "^10.1.3",
    "vitest": "^2.1.9"
  }
}
